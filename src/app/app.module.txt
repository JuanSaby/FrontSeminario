import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
import { RouterModule } from '@angular/router'; // Importa RouterModule
import { PresAnimalComponent } from './pres-animal/pres-animal.component'; // Asegúrate de tener la ruta correcta
import { HttpClientModule } from '@angular/common/http';
import { HomeComponent } from './home/home.component';

@NgModule({
  declarations: [
    PresAnimalComponent,
    // otros componentes si los tienes
  ],
  imports: [
    BrowserModule,
    RouterModule.forRoot([
      // Aquí tus rutas
      { path: 'home', component: HomeComponent },  // Página principal con la lista de animales
      { path: 'detalle-animal/:id', component: PresAnimalComponent },
    ]),
    // otros módulos
  ],
  providers: [],

})
export class AppModule { }
